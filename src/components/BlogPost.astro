---
const { title, url } = Astro.props
---

<style>
    a {
        color: var(--color-text);
        font-size: 1.5em;
    }

    a:hover {
        color: var(--color-text-highlight);
    }

    li {
        padding-block: 0.2em;
    }
</style>

<li>
    <a href={url}>{title}</a>
</li>

